experiment_name: "03b_atc_mlp"

model:
  architecture: "gcn"
  hidden_dim: 128
  num_layers: 5
  dropout: 0.2
  heads: 3
  pooling: "mlp"

training:
  epochs: 50
  batch_size: 32
  lr: 0.001
  seed: 42

data:
  train_tasks: "atc"
  eval_tasks: "atc"
