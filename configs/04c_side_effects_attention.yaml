experiment_name: "04c_side_effects_attention"

model:
  architecture: "gcn"
  hidden_dim: 128
  num_layers: 5
  dropout: 0.2
  heads: 3
  pooling: "attention"

training:
  epochs: 50
  batch_size: 32
  lr: 0.001
  seed: 42

data:
  train_tasks: "side_effects"
  eval_tasks: "side_effects"
