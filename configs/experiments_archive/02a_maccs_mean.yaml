experiment_name: "02a_maccs_mean_gat"

model:
  architecture: "gat"
  hidden_dim: 128
  num_layers: 5
  dropout: 0.2
  heads: 3
  pooling: "mean"

training:
  epochs: 500
  batch_size: 32
  lr: 0.0001
  seed: 42

data:
  train_tasks: "maccs"
  eval_tasks: "maccs"
