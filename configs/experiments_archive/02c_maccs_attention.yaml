experiment_name: "02c_maccs_attention_gat"

model:
  architecture: "gat"
  hidden_dim: 128
  num_layers: 5
  dropout: 0.2
  heads: 3
  pooling: "attention"

training:
  epochs: 500
  batch_size: 32
  lr: 0.0001
  seed: 42

data:
  train_tasks: "maccs"
  eval_tasks: "maccs"
